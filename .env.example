# === Agent 백엔드 (agent-backend/.env 로 복사) ===
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
AGENT_PORT=8000
CORS_ALLOW_ORIGIN=http://localhost:3000

# === 클라이언트 (client/.env.local 로 복사) ===
NEXT_PUBLIC_AGENT_URL=http://localhost:8000
